## Context

用户确认点房东是上一步「我的」导致的：4 格右下 ≈ 5 格「房东」。

## Goals / Non-Goals

**Goals:** 出现「房东」后不再点右下；只点智能门锁卡片。

**Non-Goals:** 不改门锁页圆钮。

## Decisions

1. `isHome` 在出现「房东」时为假。
2. 正向阶段：点过我的或见到租约底栏后，不再执行我的点击。
3. 去掉底栏第 4 格坐标点击。
4. 租约底栏上若无障碍读不到「智能门锁」，点右上卡片约 (75%, 28%)。
