## Why

桌面图标是简单绿锁线框，不够精致，需要美化。

## What Changes

- 新的自适应启动图标：深色底 + 薄荷绿开锁图案。
- 清单改用 mipmap 启动图标。

## Capabilities

### Modified Capabilities
- `one-tap-unlock`: 应用图标更换为美化后的门锁图标。

## Impact

- `app/src/main/res/` 图标资源、`AndroidManifest.xml`、APK。
